<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/bg"
    tools:context="com.example.danieltilley.lastfmtoptracks.MainActivity">

    <LinearLayout
        android:id="@+id/linearlayout"
        android:orientation="horizontal"
        android:background="@color/header_block_color"
        android:layout_width="fill_parent"
        android:layout_height="25dp">

        <TextView
            android:id="@+id/selectedLocationText"
            android:textColor="#fff"
            android:text="Selected City:"
            android:textSize="16sp"
            android:textStyle="bold"
            android:paddingLeft="5dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

    </LinearLayout>

    <ListView
        android:id="@+id/list"
        android:layout_below="@id/linearlayout"
        android:dividerHeight="2dp"
        android:divider="@color/custom_actionbar"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_marginTop="10dp"></ListView>

</RelativeLayout>
